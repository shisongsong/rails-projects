<% if @feed_items.any? %>
  <ol class="microposts">
    <%= will_paginate @feed_items, :next_label => "下一页", :previous_label => "上一页", :inner_window => 3 %>
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
    <%= will_paginate @feed_items, :next_label => "下一页", :previous_label => "上一页", :inner_window => 3 %>
  </ol>
<% end %>
